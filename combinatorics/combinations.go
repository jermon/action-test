package combinatorics

///////////////////////////////////////////////////////////////////////////////

///////////////////////////////////////////////////////////////////////////////

// NrOfCombinations returns the number of combinations r from n. nCr
func NrOfCombinations(r, n int) int {
	return factorial(n) / (factorial(n-r) * factorial(r))
}
