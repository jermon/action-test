name: Go

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - name: env
      run: env
      
    - name: Checkout code
      uses: actions/checkout@v2
      #with:
      #  path: src

    - name: pwd_ls
      run: |
        pwd
        
    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.15

    - name: pwd_ls
      run: |
        mkdir -p $GOPATH/src
        ln -s $GITHUB_WORKSPACE $GOPATH/src 
        ls $GOPATH/src
        
    - name: Go env GOPATH
      run: go env GOPATH

    - name: Build
      run: |
        export GOPATH=GITHUB_WORKSPACEgo
        go build -v ./...

    - name: Test
      run: go test -v ./...
